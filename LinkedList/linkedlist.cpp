#include <iostream>
using namespace std;

// To create a linked list first we have to create the node data type.
class Node {
    int value;
    Node *next;

    Node(int value) {
        this->value = value;
        this->next = nullptr;
    } 
};

int main() {
    
    return 0;
}